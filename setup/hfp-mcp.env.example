# Extra args for the HFP MCP service. After editing: systemctl --user restart hfp-mcp
HFP_MCP_OPTS="--port 8000 --status-port 8001"

# Restrict who may connect (repeatable); omit for open LAN access:
#HFP_MCP_OPTS="--port 8000 --status-port 8001 --allowed-host raspberrypi.local:8000"
